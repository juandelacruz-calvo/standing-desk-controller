[project]
name = "standing-desktop-controller"
version = "0.1.0"
description = "DIY standing desk height controller using Wemos D1 Mini (ESP8266), HC-SR04 ultrasonic sensor, and programmable memory presets"
requires-python = ">=3.14"
dependencies = [
    "platformio>=6.1.19",
]
